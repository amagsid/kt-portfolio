import React from 'react';

const NavBar = () => {
  return (
    <nav className='flex items-center justify-center text-3xl font-thin rounded-full absolute t-0 left-0 w-full bg-zinc-800 bg-opacity-30 backdrop-blur-3xl h-[5rem]'>
      Nav bar
    </nav>
  );
};

export default NavBar;
